{% extends 'base.html.twig' %}

{% block title %} {{room.title}} {% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1> Sujet</h1>

    <div>
        <div>
            <h3>{{room.title}}</h3>
            <p>{{room.content}}</p>
            <a href="{{ path("app_room_showOneRoom", {id:room.id}) }}">Revenir à tous les sujets !</a>
        </div>
    </div>
</div>
{% endblock %}